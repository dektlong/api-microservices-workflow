# this is a mockup file to illustrate future API validator concepts 
metadata:
  name: {{app-name}}-external-api-validator
spec:
  - validators: # notRequired, alwaysRequired, updateOnly
    sso: alwaysRequired 
    model: updateOnly
    path: alwaysRequired
    method: alwaysRequired
    rateLimit: alwaysRequired
    circuitBreaker: alwaysRequired

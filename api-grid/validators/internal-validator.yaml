# this is a mockup file to illustrate future API validator concepts 
metadata:
  name: {{app-name}}-internal-api-validator
spec:
  - validators: # notRequired, alwaysRequired, updateOnly
    sso: updateOnly 
    model: updateOnly
    path: alwaysRequired
    method: alwaysRequired
    rateLimit: updateOnly
    circuitBreaker: notRequired

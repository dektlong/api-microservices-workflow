
Gateway supch
HA gateway - replace instance count
SSO OpenID identity provide provider Google, Okta, Microsoft, Amazo generic OpenID add secret and sso file example)
API portal includes - project APIs, LoB APIs, off platform (‘brownfield’)


command:
  name: new
  description: Create a new micro-gateways for online Stores
  options:
     - name: sso-config
       description: Add configuration to support protected routes on your gateways
       dataType: boolean
       defaultValue: true
       inputType: toggle
     - name: api-keys
       description: Location of the api-key provider
       defaultValue: 
       paramLabel: WORD
       inputType: text     # TEXT
#

sources:
  include:
    - name: everything
      condition: true
      patterns: [ '**/*' ]
  exclude:
    - name: test-container-filers
      condition: "!#useTestContainers"
      patterns: [ '**/*TestContainer*' ]
    - name: hidden-files
      condition: true
      patterns:
        - ".idea/**"
        - ".git/**"
        - "target/**"

symbols:
  derived:
    - name: camelCaseProjectName
      expression: "#helper.camelCase(#projectName)"


transformations:
  replace:
    - name: 'Replace - project creation time in version controller'
      regex: '1 January 1970 00:00:00 UT'
      with: '#now'
    - name: 'Replace - project name in version controller'
      regex: 'name-mysqlapp'
      with: '#camelCaseProjectName'
